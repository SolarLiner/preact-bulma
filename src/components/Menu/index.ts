import MenuWidget, {MenuItem, MenuLabel, MenuList, MenuSubList} from "./Menu";

const Menu = {
  Menu: MenuWidget,
  Label: MenuLabel,
  List: MenuList,
  SubList: MenuSubList,
  Item: MenuItem
};

export default Menu;
